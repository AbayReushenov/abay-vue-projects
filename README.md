# Vue Playground: Dynamic prototypes and projects for UI/UX experiments.

[![Vue 3](https://img.shields.io/badge/Vue.js-3-4FC08D?logo=vuedotjs&logoColor=white)](https://vuejs.org/)
[![Composition API](https://img.shields.io/badge/API-Composition-35495E)](https://vuejs.org/guide/extras/composition-api-faq)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

**Песочница для исследований и творчества на Vue 3.** Этот репозиторий — коллекция прототипов, концептов и законченных мини-проектов, где я исследую границы пользовательского интерфейса и опыта.

Здесь нет производственного кода «как есть». Это лаборатория, где идеи обретают интерактивную форму, проверяются гипотезы и оттачивается мастерство работы с современным фронтендом.

---

## 🎯 Философия проекта

Каждый проект в этой песочнице решает конкретную, часто узкую, задачу в области UI/UX:
*   **Не «Как сделать?», а «А что, если?»** – Фокус на экспериментах с интерактивностью, анимацией и новыми паттернами.
*   **Изоляция концепции** – Одна ветка, один проект — одна исследуемая тема (например, drag-and-drop, виртуальный список, кастомный переход).
*   **Скорость и гибкость** – Быстрый старт с Vite, минимальная конфигурация, чтобы сосредоточиться на визуальной и логической части.

## 🗂️ Структура репозитория

Каждая директория в `/projects` — это самостоятельный, изолированный эксперимент.

## 🚀 Как запустить эксперимент

Все проекты собраны на **Vite**.
Проект использует моноструктуру для простоты. Все эксперименты находятся в директории `/projects_[##]`. Каждый подкаталог — это самостоятельный, изолированный проект-концепт.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/AbayReushenov/abay-vue-projects.git
    cd abay-vue-projects/projects/[project##]
    ```
2.  **Установите зависимости:**
    ```bash
    npm install
    ```
3.  **Запустите режим разработки:**
    ```bash
    npm run dev
    ```
4.  **Откройте навигатор проектов** (обычно `http://localhost:5173`), чтобы запустить конкретный эксперимент.

Чтобы собрать конкретный проект для предпросмотра:
```bash
npm run build -- --mode [project-name]
```

## 🧩 Как добавить новый эксперимент
Все эксперименты являются самостоятельными проектами Vue, созданными через официальный шаблон. Это гарантирует чистоту и изоляцию каждого концепта.

1. Создание нового проекта-эксперимента
Перейдите в директорию /projects и выполните команду, указав осмысленное имя (например, 04-animated-timeline):

```
bash
cd projects
npm create vue@latest [название-эксперимента]
```
Пример:
```
bash
npm create vue@latest 04-animated-timeline
```
Во время создания выберите необходимые опции (TypeScript, Pinia, Vitest и т.д.) в зависимости от задач эксперимента.

2. Интеграция в общий проект
После создания:

Удалите лишние файлы: Внутри новой папки проекта можно удалить файлы README.md, package.json (если хотите управлять зависимостями из корня) и другие стандартные файлы, не относящиеся непосредственно к коду эксперимента.

Перенесите логику: Основной код вашего эксперимента (компоненты, страницы, логику) разместите внутри созданной папки, следуя стандартной структуре Vue-проекта.

Добавьте в навигатор: Чтобы новый эксперимент появился в общей навигации, добавьте информацию о нём в главный файл-навигатор, расположенный в корне репозитория (например, src/App.vue).

3. Запуск и проверка
Все проекты используют общие корневые зависимости. После создания эксперимента установите/обновите зависимости из корня репозитория и запустите dev-сервер:

```
bash
# Из корневой директории vue-playground/
npm install
npm run dev
```
Новый эксперимент станет доступен через главную навигационную страницу.

🗂️ Рекомендуемая структура папки эксперимента
```text
/projects/04-animated-timeline/
├── src/
│   ├── assets/      # Статические ресурсы (изображения, локальные шрифты)
│   ├── components/  # Дочерние компоненты, специфичные для этого эксперимента
│   ├── App.vue      # КОРНЕВОЙ компонент этого эксперимента
│   └── main.js      # Точка входа (чаще всего остаётся без изменений)
├── index.html       # Шаблон HTML
└── ...              # Другие конфигурационные файлы (vite.config.js, eslint и т.д. при необходимости)
```
Важно: Главный App.vue внутри папки эксперимента должен содержать полную, рабочую версию этого концепта.

💡 Зачем это нужно?
Такой подход (npm create vue@latest) — это лучшая практика, потому что он:

Стандартизирует старт каждого нового эксперимента.

Гарантирует актуальность и совместимость с последними версиями Vue и экосистемы.

Экономит время на ручную настройку сборки, линтинга и тестирования.

Сохраняет изоляцию — вы можете выбрать разные опции (с TS или без) для разных экспериментов.


## ПРИМЕР
1. Инициализация проекта
Открой терминал и выполни команду для скаффолдинга проекта:

```bash
npm create vue@latest nabokov-shuffle
```
В процессе установки выбери следующие опции (ключевой момент — Pinia):

| Опция                | Выбор | Комментарий                                                  |
| -------------------- | ----- | ------------------------------------------------------------ |
| Add TypeScript?      | Yes   | Строгая типизация для наших Card и Store.                    |
| Add JSX Support?     | No    | Будем использовать стандартные .vue шаблоны (idiomatic Vue). |
| Add Vue Router?      | Yes   | Пригодится для переключения режимов (Board/List/Settings).   |
| Add Pinia?           | Yes   | Главная цель. Заменяет Vuex.                                 |
| Add Vitest?          | No    | Пока пропустим, если захочешь тесты — добавим позже.         |
| Add ESLint/Prettier? | Yes   | Стандарт гигиены кода.                                       |


2. Установка зависимостей
Переходим в папку и ставим пакеты:

```bash
cd nabokov-shuffle
npm install

```

3. Дополнительные утилиты
Нам понадобится пара небольших библиотек для реализации логики "карточек":

nanoid: Для генерации уникальных ID карточек (легче и быстрее uuid).

sass: Для удобной стилизации (опционально, но рекомендую для вложенности стилей).

```
npm install nanoid
npm install -D sass
```
4. Проверка конфигурации (Main Entry)
Vite автоматически настраивает Pinia, но давай убедимся, что main.ts выглядит корректно. Открой src/main.ts.

Он должен выглядеть примерно так (обрати внимание на createPinia):

```typescript
import './assets/main.css'

import { createApp } from 'vue'
import { createPinia } from 'pinia' // <--- Важный импорт

import App from './App.vue'
import router from './router'

const app = createApp(App)

app.use(createPinia()) // <--- Подключение Pinia к инстансу Vue
app.use(router)

app.mount('#app')

```

5. Подготовка структуры папок
Давай сразу очистим проект от "Hello World" компонентов и создадим нужные директории под нашу архитектуру.

Удали всё из папки src/components/ (кроме папок, если есть).

Удали всё содержимое src/assets/base.css и main.css (начнем с чистого листа), или оставь минимальный сброс стилей.

Создай следующую структуру внутри src/:

```text
src/
├── assets/
├── components/
│   ├── TheDesk.vue       # Основное рабочее поле (Smart Component)
│   └── NoteCard.vue      # Отдельная карточка (Dumb Component)
├── stores/
│   ├── shoebox.ts        # Data Store (хранит сами заметки)
│   └── desk.ts           # UI Store (хранит настройки отображения)
├── types/                # TypeScript интерфейсы
│   └── index.ts
├── App.vue
└── main.ts

```
